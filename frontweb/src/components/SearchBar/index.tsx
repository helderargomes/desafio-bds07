import './styles.css'

const SearchBar = () => {
  return (
    <div className="base-card search-card">
      <form action="">
        <input placeholder="Digite sua busca" />
      </form>
    </div>
  );
};

export default SearchBar;
